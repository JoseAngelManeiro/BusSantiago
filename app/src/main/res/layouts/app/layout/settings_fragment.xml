<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
  android:layout_width="match_parent"
  android:layout_height="wrap_content"
  android:orientation="vertical"
  android:padding="@dimen/default_padding"
  tools:context="org.galio.bussantiago.features.settings.SettingsFragment">

  <TextView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:text="@string/home_screen_settings_header_title"
    android:textSize="@dimen/default_text_size" />

  <RadioGroup
    android:id="@+id/home_screen_radio_group"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="@dimen/default_margin">

    <RadioButton
      android:id="@+id/favorites_radio_button"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/favorite_screen_settings_option_title"
      android:textSize="@dimen/small_text_size" />

    <TextView
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="@string/favorite_screen_settings_option_subtitle"
      android:layout_marginStart="33dp"
      android:textSize="12sp" />

    <RadioButton
      android:id="@+id/lines_radio_button"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:text="@string/lines_screen_settings_option_title"
      android:layout_marginTop="@dimen/small_margin"
      android:textSize="@dimen/small_text_size" />

  </RadioGroup>

</LinearLayout>
